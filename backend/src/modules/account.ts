import { writeFileSync, readFileSync } from "node:fs"

class UserData {
    constructor(user: string, pass: string) {

    }
}

export const authorization = () => {

}

export const getSession = (session: string, user_id: string) => {

}